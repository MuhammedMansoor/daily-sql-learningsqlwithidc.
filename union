1. Combine patient names and staff names into a single list.
2. Create a union of high satisfaction patients (>90) and low satisfaction patients (<50).
3. List all unique names from both patients and staff tables.

SOL
1, USE Challenge;
SELECT name, 'Patient' AS source FROM patients
UNION ALL SELECT staff_name, 'Staff' AS source FROM staff;

2, USE Challenge;
SELECT patient_id,"High Satisfaction" AS Sati,satisfaction from patients where(satisfaction>90)
UNION ALL
SELECT patient_id,"Low Satisfaction" AS Sati,satisfaction from patients where(satisfaction<50)
order by satisfaction asc;

3, USE Challenge;
SELECT distinct(name) as names from patients
UNION ALL
SELECT distinct(staff_name) as names from staff;


 Create a comprehensive personnel and patient list showing: identifier (patient_id or staff_id), full name, type ('Patient' or 'Staff'), and associated service. 
Include only those in 'surgery' or 'emergency' services. Order by type, then service, then name.

USE Challenge;
SELECT "patient_id" as identifyer,name as names,"Patient" as type, service from patients where service in ("surgery","emergency")
UNION ALL
SELECT "staff_id" as identifyer,staff_name as names,"Staff" as type, service from staff where service in ("surgery","emergency")
order by type,service,names;
